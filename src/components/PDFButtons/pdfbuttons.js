import React from "react"
import * as styles from "./pdfbuttons.module.scss"

const ButtonGuides = ({ text }) => {
  return <div className={styles.btn}>{text}</div>
}

export default ButtonGuides
